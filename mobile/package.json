{
  "name": "lantern-toeic-mobile",
  "private": true,
  "version": "0.2.3",
  "type": "module",
  "scripts": {
    "build": "node ../node_modules/vite/bin/vite.js build --config vite.config.ts",
    "sync": "npm run build && cap sync",
    "android": "cap open android",
    "ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
