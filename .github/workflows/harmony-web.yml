name: HarmonyOS shared asset check
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['art/**', 'harmony/**', 'app/**', 'lib/**', 'components/**', 'content/**', 'public/**', 'mobile/**', 'package-lock.json', '.github/workflows/harmony-web.yml']
  pull_request:
    paths: ['art/**', 'harmony/**', 'app/**', 'lib/**', 'components/**', 'content/**', 'public/**', 'mobile/**', 'package-lock.json', '.github/workflows/harmony-web.yml']
permissions:
  contents: read
concurrency:
  group: harmony-web-${{ github.ref }}
  cancel-in-progress: true
jobs:
  shared-assets:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Check native build helpers (no SDK compilation)
        run: |
          node --test harmony/tests/toolchain.test.mjs
          node --test harmony/tests/native-downloads.test.cjs harmony/tests/entry-ability.test.cjs
          python3 -B -m unittest discover -s harmony/tests -p 'test_*.py'
      - run: npm --prefix harmony run sync:web
      - run: npm --prefix harmony run check:web
      - name: Upload verified web resources (not an installable HAP)
        uses: actions/upload-artifact@v7
        with:
          name: lantern-harmony-web-resources-${{ github.run_number }}
          path: harmony/entry/src/main/resources/rawfile/web/
          if-no-files-found: error
          retention-days: 14
